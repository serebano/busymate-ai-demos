{
  "$comment": "STATUS: catalog, backend connector, tenant (identity provider tested 6/6, connector configured, knowledge published), Script Manager embed + WebMCP tools are all LIVE (see README.md). Grounded product chat is proven live. NOT proven: identified order lookup — the tool call reaches the connector and is correctly refused 'not signed in' because set_connector_actor_verifier (registers this connector's actor secret with the platform) returns native_action_unavailable on the live busymate.ai/mcp right now. Also blocked: the real BigCommerce storefront is 'prelaunch' (control-panel-only Launch step, no API path) — verified instead on this backend's own /preview page. status stays 'building' until both close.",
  "name": "bigcommerce",
  "subdomain": "bigcommerce",
  "title": "BigCommerce store",
  "tagline": "Copperfield Kitchen Co. — a real BigCommerce trial store with a live catalogue, real orders, and (in progress) a grounded assistant.",
  "$comment2": "type:\"dynamic\" (like woo/wordpress/ghost, not static+backend-hybrid): the real demo IS the owner's live BigCommerce storefront (Script Manager embed) — bigcommerce.demo.busymate.ai is this backend serving /mcp, the six agent-ready files and a minimal landing page directly (Node, no separate public/ docroot). scripts/deploy.sh therefore skips it; see sites/wordpress or sites/woo for the same shape.",
  "type": "dynamic",
  "assistant": "demo-bigcommerce",
  "status": "building",
  "order": 13,
  "icon": "bigcommerce",
  "docs": "https://busymate.ai/docs/guides/bigcommerce",
  "code": "https://github.com/serebano/busymate-ai-demos/tree/main/bigcommerce",
  "backend_port": 8110,
  "store": {
    "store_hash": "wflwr11fds",
    "control_panel": "https://store-wflwr11fds.mybigcommerce.com/manage/dashboard",
    "storefront": "https://12zero784.mybigcommerce.com",
    "api_base": "https://api.bigcommerce.com/stores/wflwr11fds/v3/",
    "api_account_name": "copperfield-demo-connector",
    "vault_secrets": ["BIGCOMMERCE_DEMO_CLIENT_ID", "BIGCOMMERCE_DEMO_CLIENT_SECRET", "BIGCOMMERCE_DEMO_ACCESS_TOKEN"]
  }
}
